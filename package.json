{
  "name": "theaiincveil",
  "version": "1.0.0",
  "private": true,
  "description": "Veil — provider-agnostic data protection, classification, abstraction, and rehydration framework for AI-enabled applications.",
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "build": "nx run-many -t build",
    "test": "nx run-many -t test",
    "typecheck": "nx run-many -t typecheck"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "nx": "20.3.0",
    "tsup": "^8.3.5",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "nx"]
  }
}
