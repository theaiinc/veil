{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "veil-presidio",
  "sourceRoot": "packages/veil-presidio/src",
  "projectType": "library",
  "implicitDependencies": ["veil"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": { "command": "tsup", "cwd": "packages/veil-presidio" },
      "outputs": ["{projectRoot}/dist"]
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "vitest run", "cwd": "packages/veil-presidio" }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": { "command": "tsc --noEmit -p tsconfig.json", "cwd": "packages/veil-presidio" }
    }
  }
}
